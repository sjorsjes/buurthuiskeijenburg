---
import FotoAlbum from '../../_fotoalbum.astro';

export const title = "Ut groot Keijenbal 2025"

const imageImports = import.meta.glob('../../../../../public/images/fotoalbums/2025/ut groot keijenbal 2025/*.{jpeg,jpg,png,gif}', { eager: true });

const images = Object.values(imageImports).map((img) => img.default);
---

<FotoAlbum title={title} images={images} />

